services:
  - type: web
    name: ai-study-buddy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python demo.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
      - key: FLASK_ENV
        value: production
